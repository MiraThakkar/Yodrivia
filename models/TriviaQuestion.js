class TriviaQuestion {
    constructor(questionType, question, options, answer) {
        this.questionType = questionType;
        this.question = question;
        this.options = options;
        this.answer = answer;
    }
}